package CDD.background;

import org.joml.Matrix4f;

import CDD.shader.BaseShader;

/**
 * 
 * This is an extension on top of the BaseShader.
 * All shaders should normally use BaseShader as
 * a parent, since BaseShader contains the majority
 * of a shader's essential barebones.
 * </br><br>
 * Later on, once an Uber shader system has been 
 * established, we'll be able to add extensions
 * on top of the BaseShader, so that we can achieve
 * </br><br>
 * "One shader to render them all"
 * 
 */
public class BackgroundShader extends BaseShader {
    
}
